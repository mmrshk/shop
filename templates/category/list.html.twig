{% extends 'base.html.twig' %}
{% block title %}
    Категории - {{ parent() }}
{% endblock %}
{% block body %}
    {% for category in categories %}
        <a href="{{ path('category_show', {'slug':category.id}) }}">
            {{ category.name }}
        </a>
        <hr>
    {% endfor %}
{% endblock %}